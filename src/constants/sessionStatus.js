/**
 * Engagement session status constants
 */
export const SessionStatus = {
  WAITING: 'waiting',
  ACTIVE: 'active', 
  ROLLING: 'rolling',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled'
}

export default SessionStatus
