/** @typedef {import('@shared/types/dice.js').DieSize} DieSize */

/**
 * Preview-only die metadata for skill UI.
 * These flags are not part of core roll-domain die identity.
 * @typedef {Object} SkillPreviewDie
 * @property {DieSize} dieSize
 * @property {boolean} [isAdded]
 * @property {boolean} [isSubtracted]
 * @property {string} [cssClass]
 */

export {}
